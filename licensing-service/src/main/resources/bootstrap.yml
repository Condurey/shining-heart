server:
  port: 8911

spring:
  application:
    name: licensingservice
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true
#      discovery:
#        enabled: true
#        serviceId: configserver
